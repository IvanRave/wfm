define(['jquery', 'ko'], function ($, ko) {
    'use strict';

    // Widget layout
    function WroupWidgout(data) {
        var self = this;
        data = data || {};
    }

    return WroupWidgout;
});
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <title>{{lang.wfm}}: {{pageTitle}}</title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="{{rootUrl}}css/bootstrap.css" rel="stylesheet" media="screen">
    <link href="{{rootUrl}}css/bootstrap-theme.css" rel="stylesheet" media="screen">
    <link href="{{rootUrl}}css/main.css" rel="stylesheet" />
    {{#headStyles }}
    <link href="{{.}}" rel="stylesheet" />
    {{/headStyles}}
</head>
<body>
    <!--[if lt IE 9]><p class="chromeframe">You are using an <strong>outdated</strong> browser. Please upgrade your browser.</p><![endif]-->
    <div class="spinner"></div>
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
        <div class="navbar-header">
            <button class="navbar-toggle" type="button" data-toggle="collapse" data-target=".navbar-ex1-collapse">
                <span class="sr-only">Nav</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{rootUrl}}{{conf.defPage}}">{{lang.wfm}}</a>
        </div>
        <div class="collapse navbar-collapse navbar-ex1-collapse">
            <ul class="nav navbar-nav">
                <li><a href="{{rootUrl}}{{conf.defPage}}">{{capitalizeFirst lang.home}}</a></li>
                <li><a href="{{rootUrl}}pricing/calculator/{{conf.defPage}}">{{capitalizeFirst lang.pricing}}</a></li>
                <li><a href="{{rootUrl}}about/{{conf.defPage}}">{{capitalizeFirst lang.aboutWfm}}</a></li>
                <li><a href="{{rootUrl}}contacts/{{conf.defPage}}">{{capitalizeFirst lang.contacts}}</a></li>
            </ul>
            <ul class="nav navbar-nav pull-right">
                <li><a href="{{syst.cabinetUrl}}">Cabinet</a></li>
            </ul>
        </div>
    </nav>
    <div class="wfm-content-wrap">
        {{> body }}
    </div>
    <footer class="navbar-fixed-bottom navbar-inverse">
        <div class="footer-logo-wrap">
            <div class="footer-logo"></div>
        </div>
    </footer>
    <!-- Load main script file for page or default (with auth logic only) -->
    <script data-main="{{rootUrl}}js/rqr-{{default rqrId 'default'}}" src="{{rootUrl}}js/require.js"></script>
</body>
</html>
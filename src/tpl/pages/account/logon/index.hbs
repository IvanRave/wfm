---
page_title: Sing in
---
<div class="wfm-content-pad" id="logon-project-wrap">
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <div class="row" ng-controller="LogonCtrl">
                <h3>{{page_title}}:</h3>
                <form class="form-horizontal" name="logonForm">
                    <div class="form-group" ng-class="{'has-error': logonForm.Email.$invalid}">
                        <label class="control-label col-sm-2">{{titleize lang.email}}</label>
                        <div class="col-sm-6">
                            <input type="text" name="Email" required autofocus class="form-control" ng-model="usver.Email" />
                        </div>
						<!-- TODO: add min and max limit -->
                        <div class="col-sm-4 has-error">
                            <span ng-show="logonForm.Email.$error.required" class="help-block">required</span>
                        </div>
                    </div>
                    <div class="form-group" ng-class="{'has-error': logonForm.Password.$invalid}">
                        <label class="control-label col-sm-2">Password</label>
                        <div class="col-sm-6">
                            <input type="text" name="Password" required class="form-control" ng-model="usver.Password" />
                        </div>
                        <!-- TODO: add min and max limit -->
                        <div class="col-sm-4 has-error">
                            <span ng-show="logonForm.Password.$error.required" class="help-block">required</span>
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="row">
                            <div class="col-sm-2"></div>
                            <div class="col-sm-4">
                                <div class="checkbox">
                                    <label>
                                        <input name="RememberMe" type="checkbox" ng-model="usver.RememberMe" /> Remember me
                                    </label>
                                </div>
                            </div>
                            <div class="col-sm-2">
                                <button ng-click="tryAuth()" ng-disabled="logonForm.$invalid" class="btn btn-primary pull-right">{{page_title}}</button>
                            </div>
                        </div>
                    </div>
                </form>
                <h4>
                    or <a href="{{syst.register_url}}">register a new company</a>
                </h4>
            </div>
        </div>
    </div>
</div>